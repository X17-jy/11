{
  "build": {
    "command": "pip install -r requirements.txt && pip install gunicorn",
    "outputDirectory": ".",
    "env": {
      "PYTHON_VERSION": "3.11",
      "NODE_VERSION": "18"
    }
  }
}
